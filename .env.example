# Copy to .env and fill in values from your Supabase project (Settings > API).

# Project URL, e.g. https://xxxx.supabase.co
SUPABASE_URL=

# Use the anon key for client-style access, or service_role for server-side only (keep secret).
SUPABASE_KEY=
